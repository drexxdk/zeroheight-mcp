{
  "servers": {
    "zeroheight-scraper": {
      "command": "npx",
      "args": [
        "mcp-remote",
        //"https://zeroheight-mcp.vercel.app/api/mcp",
        "http://localhost:3000/api/mcp",
        "--header",
        "Authorization: Bearer ${MCP_API_KEY}"
      ],
      "env": {
        "MCP_API_KEY": "your-local-api-key-here"
      }
    }
  }
}
