# ZeroHeight Design System Scraper MCP Server
# Copy this file to .env.local and fill in your values

# ZeroHeight project URL to scrape
ZEROHEIGHT_PROJECT_URL=https://your-project.zeroheight.com/p/project-id

# Password for the ZeroHeight project (if required)
ZEROHEIGHT_PROJECT_PASSWORD=your-password

# NOTE: MCP_API_KEY should NOT be set in .env.local for security reasons.
# Instead, set it as an environment variable in your Vercel deployment:
# https://vercel.com/dashboard -> Your Project -> Settings -> Environment Variables